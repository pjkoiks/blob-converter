services:
  - type: web
    name: blob-converter
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn blob_converter_app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
